# Warehouse Management API

REST API для управления складом продуктов.

## 🚀 Технологический стек

*   **Language:** Go (1.24+)
*   **Framework:** [Gin Gonic](https://github.com/gin-gonic/gin)
*   **Database:** PostgreSQL
*   **Architecture:** Clean Architecture (Entities, Use Cases, Repository, Delivery)
*   **Migrations:** [golang-migrate](https://github.com/golang-migrate/migrate)
*   **Containerization:** Docker & Docker Compose
*   **Logging:** Structured logging с использованием `slog` и кастомным цветным хендлером.
*   **Testing:** Unit-тесты бизнес-логики (Testify, Mock).

---

## 🏗 Архитектура

Проект строго следует принципам **Clean Architecture**:
*   `internal/domain`: Описывает бизнес-сущности (Product) и интерфейсы репозиториев/юзкейсов. Не зависит от внешних библиотек.
*   `internal/usecase`: Содержит бизнес-логику и правила валидации.
*   `internal/repository`: Отвечает за работу с данными (PostgreSQL).
*   `internal/delivery`: Слой взаимодействия с внешним миром (HTTP хендлеры на Gin).
*   `pkg/logger`: Кастомный логгер для удобной отладки.

---

## 🛠 Запуск проекта

### Предварительные требования
*   Установленный [Docker](https://www.docker.com/get-started) и [Docker Compose](https://docs.docker.com/compose/install/).

### Быстрый старт
1.  Склонируйте репозиторий:
    ```bash
    git clone https://github.com/ultrabor/warehouse-api.git
    cd warehouse-api
    ```

2.  Изменити файл `.env` в корне проекта (или используйте значения по умолчанию):
    ```env
    POSTGRES_USER=postgres
    POSTGRES_PASSWORD=password
    POSTGRES_DB=warehouse
    DB_URL=postgres://postgres:password@db:5432/warehouse?sslmode=disable
    PORT=8080
    ```

3.  Запустите проект с помощью Docker Compose:
    ```bash
    make docker-rebuild
    ```
    *Эта команда соберет приложение, поднимет базу данных PostgreSQL и автоматически применит миграции.*

---

## 🧪 Тестирование

Для запуска Unit-тестов бизнес-логики используйте команду:
```bash
make test
```
Тесты проверяют слой `UseCase`, используя mock репозитория, что гарантирует независимость тестов от базы данных.

---

## 📡 API Эндпоинты

| Метод | Маршрут | Описание |
| :--- | :--- | :--- |
| `POST` | `/products` | Добавление нового продукта |
| `GET` | `/products` | Получение списка всех продуктов |
| `GET` | `/products/:id` | Получение информации о продукте по ID |
| `PUT` | `/products/:id` | Обновление информации о продукте |
| `DELETE` | `/products/:id` | Удаление продукта |

### Примеры запросов
В корне проекта находится папка `examples`. Вы можете использовать его в VS Code (расширение REST Client) или GoLand для быстрого тестирования всех методов API.

---

## 📝 Команды Makefile

*   `make test` - запуск unit-тестов.
*   `make up` - запуск контейнеров в фоне.
*   `make rebuild` - пересборка и запуск проекта (рекомендуется).
*   `make logs` - посмотреть логи приложения

---
